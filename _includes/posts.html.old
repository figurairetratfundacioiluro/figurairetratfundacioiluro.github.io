    <!-- Filosofia Section -->
    <section id="portfoli" class="content-section text-center no-padding">
        <div class="portfoli-section">
            <div class="yellow-bg">
                <div class="container">
                  <h2>Portfoli d'Figura<span class="copulativa">i</span><span class="light">Retrat</span></h2>
                    <div class="posts">
                      {% for post in site.categories['portfoli'] %}
                      <div class="post">

                        {{ post.excerpt }}    

                      <h3 class="post-title">
                          <a href="{{ post.url }}">
                            {{ post.title }}
                          </a>
                        </h3>
                        
                        </div>
                      {% endfor %}
                    </div>
                    <br style="clear:both">
                </div>
            </div>
        </div>
    </section>